/**
* @file:   dataconfig_gameparamsconfig_conf.proto
* @author: jameyli <jameyli AT tencent DOT com>
* @brief:  这个文件是通过工具自动生成的，建议不要手动修改
*/

package dataconfig;

message GAMEPARAMSCONFIG_CONF{
    /** 配置条目的编号 */
    required int32 id = 1 [default = 0];
    /** 食物分布的密度 */
    required float food_density = 2 [default = 0];
    /** 分值定义 */
    repeated int32 foods_socre_def = 3;
}

message GAMEPARAMSCONFIG_CONF_ARRAY {
    repeated GAMEPARAMSCONFIG_CONF items = 1;
}
